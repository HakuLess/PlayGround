<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapp">

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@android:drawable/sym_def_app_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ViewPagerTestActivity" />
        <activity android:name=".KeyboardTestActivity" 
            android:windowSoftInputMode="adjustNothing" />
        <activity android:name=".CoroutineDemoActivity" />
        <activity android:name=".UIKitDemoActivity" />
        <activity android:name=".ComposeDemoActivity" />
    </application>

</manifest>